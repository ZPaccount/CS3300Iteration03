<!DOCTYPE html>
<html>
  <head>
    <title>CS3300Iteration03</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <p class="navbar-text float-right fs-4">
  <% if user_signed_in? %>
    Logged in as <strong class="fs-4"><%= current_user.email %></strong>.
    <%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-link text-light btn btn-lg btn-success' %> |
    <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'navbar-link text-light btn btn-lg btn-success'  %>
  <% else %>
    <%= link_to "Sign up", new_user_registration_path, :class => 'navbar-link text-light btn btn-lg btn-success'  %> |
    <%= link_to "Login", new_user_session_path, :class => 'navbar-link text-light btn btn-lg btn-success'  %>
  <% end %>
  </p>
  <p class="notice"><%= notice %></p> 
  <p class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
